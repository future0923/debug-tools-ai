# DebugTools AI for Pi

Use the repository-level `AGENTS.md` and `docs/workflow.md` as the DebugTools method invocation and Hotswap workflow instructions.

This extension directory is a packaging placeholder until Pi-specific extension metadata is finalized.
